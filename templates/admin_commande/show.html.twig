{% extends 'base-admin.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commande.createdAt ? commande.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CarrierNom</th>
                <td>{{ commande.carrierNom }}</td>
            </tr>
            <tr>
                <th>CarrierPrix</th>
                <td>{{ (commande.carrierPrix / 100)|number_format(2, ',', '.')}} €</td>
            </tr>
            <tr>
                <th>IsPaid</th>
                <td>{{ commande.isPaid ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Delivery</th>
                <td>{{ commande.delivery }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_commande_index') }}">back to list</a>

    <a href="{{ path('admin_commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('admin_commande/_delete_form.html.twig') }}
{% endblock %}
